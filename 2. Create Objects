2. Create Objects Implement view commands to create a view object for the above tables

CREATE VIEW

create view StudentName as
select Registration_Number, Student_Name from Students;

select * from StudentName;

create view name_details as
select * from Students
where Registration_Number = ‘CSC01’;

select * from name_details;
